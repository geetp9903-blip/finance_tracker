export default function DashboardPage() {
    return (
        <div className="hidden">
            {/* The main dashboard content is handled by parallel slots (@metrics, @charts, etc.) */}
            {/* This file maps to the 'children' prop in layout.tsx */}
        </div>
    );
}
